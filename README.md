# AI Mockup Generator

Профессиональный генератор мокапов товаров с логотипами клиентов на основе Google Gemini AI.

## 🚀 Возможности

- **AI-генерация мокапов** - создание профессиональных мокапов с помощью Google Gemini 2.5 Flash
- **Поддержка различных форматов** - JPG, PNG, WebP с автоматической конвертацией RGBA → RGB
- **Оптимизация изображений** - сжатие и оптимизация для быстрой загрузки
- **Пакетная обработка** - генерация до 10 мокапов одновременно
- **Загрузка на FTP** - автоматическое сохранение на сервер
- **Аутентификация** - защита паролем
- **Адаптивный интерфейс** - работает на всех устройствах

## 📋 Требования

- Python 3.8+
- Google Gemini API ключ
- FTP сервер (опционально)

## 🛠️ Установка

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/Aizer-92/ai-mockup-generator.git
cd ai-mockup-generator
```

2. **Создайте виртуальное окружение:**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте конфигурацию:**
```bash
# Скопируйте и отредактируйте конфигурацию FTP
cp ftp_config.env.example ftp_config.env
```

5. **Запустите приложение:**
```bash
streamlit run main.py
```

## ⚙️ Конфигурация

### Переменные окружения

Создайте файл `.env` в корне проекта:

```env
# Google Gemini API
GEMINI_API_KEY=your_gemini_api_key_here

# Аутентификация
AUTH_ENABLED=true
AUTH_PASSWORD=your_secure_password

# FTP настройки (опционально)
FTP_ENABLED=true
FTP_HOST=your-ftp-host.com
FTP_USERNAME=your_username
FTP_PASSWORD=your_password
FTP_REMOTE_PATH=/public_html/mockups
```

### FTP конфигурация

Для загрузки мокапов на FTP сервер создайте файл `ftp_config.env`:

```env
FTP_ENABLED=true
FTP_HOST=search.headcorn.pro
FTP_USERNAME=your_username
FTP_PASSWORD=your_password
FTP_REMOTE_PATH=/public_html/mockups
SERVER_WEB_URL=http://search.headcorn.pro/mockups
```

## 🎨 Использование

### Одиночная генерация

1. **Загрузите товар** - выберите изображение товара (JPG, PNG, WebP)
2. **Загрузите логотип** - выберите логотип клиента
3. **Настройте параметры:**
   - Стиль мокапа (современный, классический, минималистичный)
   - Тип нанесения (вышивка, печать, лазерная гравировка)
   - Расположение логотипа (центр, левый верх, правый верх)
   - Размер логотипа (маленький, средний, большой)
   - Цвет логотипа (как на фото, черный, белый)
4. **Добавьте описание** - опционально
5. **Нажмите "Генерировать мокап"**

### Пакетная обработка

1. **Загрузите логотип** - будет использован для всех товаров
2. **Загрузите до 10 товаров** - выберите несколько изображений
3. **Настройте параметры** - аналогично одиночной генерации
4. **Нажмите "Обработать коллекцию"**

## 📁 Структура проекта

```
mockup_generator/
├── main.py                 # Основное приложение Streamlit
├── config.py              # Конфигурация и настройки
├── auth.py                # Аутентификация
├── gemini_client.py       # Клиент Google Gemini API
├── mockup_generator.py    # Основная логика генерации
├── image_processor.py     # Обработка изображений
├── ftp_uploader.py        # Загрузка на FTP сервер
├── server_storage.py      # Локальное хранение
├── cache_manager.py       # Управление кэшем
├── batch_processor.py     # Пакетная обработка
├── requirements.txt       # Зависимости Python
├── ftp_config.env         # Конфигурация FTP
└── README.md             # Документация
```

## 🔧 Технические детали

### Обработка изображений

- **Автоматическая конвертация** RGBA → RGB для совместимости с JPEG
- **Оптимизация размера** - сжатие до 512x512 пикселей
- **Качество сжатия** - 70% для баланса размера и качества
- **Поддержка форматов** - JPG, PNG, WebP

### AI Модели

- **Основная модель:** `gemini-2.5-flash-image-preview` - для генерации изображений
- **Анализ коллекций:** `gemini-2.0-flash-exp` - для анализа и рекомендаций

### Кэширование

- **Изображения** - кэшируются в session_state
- **API запросы** - кэширование результатов генерации
- **Время жизни кэша** - 24 часа

## 🌐 Деплой

### Streamlit Cloud

1. Подключите GitHub репозиторий к Streamlit Cloud
2. Добавьте переменные окружения в настройках:
   - `GEMINI_API_KEY`
   - `AUTH_PASSWORD`
   - `FTP_HOST`, `FTP_USERNAME`, `FTP_PASSWORD` (опционально)
3. Деплой автоматически запустится

### Локальный сервер

```bash
# Запуск на порту 8501
streamlit run main.py

# Запуск на другом порту
streamlit run main.py --server.port 8502
```

## 📊 Производительность

- **Время генерации:** 10-30 секунд на мокап
- **Размер изображения:** ~50-100 KB после оптимизации
- **Память:** ~200-500 MB в зависимости от количества мокапов
- **API лимиты:** Экономичные настройки для минимизации расходов

## 🔒 Безопасность

- **Аутентификация** - защита паролем
- **Валидация файлов** - проверка типов и размеров
- **Очистка данных** - автоматическое удаление временных файлов
- **Безопасное хранение** - переменные окружения для секретов

## 🐛 Устранение неполадок

### Частые проблемы

1. **Ошибка "cannot write mode RGBA as JPEG"**
   - Решение: Автоматически исправляется конвертацией в RGB

2. **Ошибка "mockups" при перегенерации**
   - Решение: Используйте кнопку "Сбросить состояние"

3. **Медленная загрузка изображений**
   - Решение: Очистите кэш браузера или используйте кнопку обновления

### Логи

Проверьте консоль браузера (F12) для отладочной информации.

## 📈 Планы развития

- [ ] Отдельная галерея мокапов на поддомене
- [ ] API для интеграции с другими системами
- [ ] Поддержка видео мокапов
- [ ] Расширенные фильтры и поиск
- [ ] Экспорт в различные форматы

## 📄 Лицензия

MIT License - см. файл LICENSE для деталей.

## 🤝 Поддержка

Для вопросов и предложений создайте issue в GitHub репозитории.

---

**Версия:** 2.0.0  
**Последнее обновление:** Сентябрь 2025  
**Статус:** Активная разработка